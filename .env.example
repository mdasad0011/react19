# Production Environment Variables
# Copy this file to .env and fill in your actual values

# Encryption secret for secure storage (useSecureStorage hook)
# Generate a strong, random key for production
# Example: openssl rand -base64 32
VITE_ENCRYPTION_SECRET=your-production-encryption-secret-here

# API Configuration
# VITE_API_BASE_URL=https://api.yourdomain.com
# VITE_API_TIMEOUT=10000

# Feature Flags
# VITE_ENABLE_ANALYTICS=true
# VITE_ENABLE_DEBUG=false

# Third-party Service Keys (if needed)
# VITE_GOOGLE_ANALYTICS_ID=GA-XXXXXXXXX
# VITE_SENTRY_DSN=https://...

# Note: Only environment variables prefixed with VITE_ are exposed to your app
# Never commit sensitive production values to version control
